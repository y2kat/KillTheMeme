using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class InfoPanel : MonoBehaviour
{
    public Text nameText;
    public Text classText;
    public Text levelText;
    
    void Update()
    {
        //if (Selector._selectecObject != null)
        //{
            //Character selectedCharacter = Selector._selectecObject.GetComponent<Character>();
            //nameText.text = selectedCharacter.GetName();
            //classText.text = selectedCharacter.GetClass();
            //levelText.text = selectedCharacter.GetLevel().ToString();
        //}
    }
}
